'use strict';

// Minimal direct-call PoC template. Fill in the target import, payload, call,
// and mocked sink assertion.

const payload = 'PROBEJS_POC_PAYLOAD';

function main() {
  // const target = require('./index.js');
  // target(payload);
  console.log('TODO: complete direct-call PoC');
  process.exitCode = 1;
}

main();

